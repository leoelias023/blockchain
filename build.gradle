wrapper {
    distributionType = Wrapper.DistributionType.ALL
    gradleVersion = '7.0'
}

allprojects {
    group = 'me.develeo'
    version = '1.0.0'

    repositories {
        mavenCentral()
    }
}
